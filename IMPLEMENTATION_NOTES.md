# 实现说明

## 已完成

- 不含真实业务数据的空白 ASIN 输入模板
- Excel/CSV/TSV 输入兼容与核心字段校验
- 1688完整Cookie读取与MTop签名
- 多关键词、多页候选采集
- 商品标题、链接、图片、供应商、页面价、起订量等候选字段
- 本地Chrome详情页打开、截图、正文证据留存
- 材质/尺寸/功能/结构/销售单元/套装/配件的匹配评分
- 人工有效/驳回覆盖
- 仅从有效候选中取最高“完整销售单元采购价”
- 美西四渠道物流取最大值
- 佣金、仓储、广告、真实毛利和投产比计算
- ROI结果、候选、输入、来源追溯、参数说明五张工作表
- Tkinter桌面界面
- Windows打包脚本
- 离线接口、计算、匹配、导出、安全边界和示例脱敏自动测试
- GitHub Actions 多版本测试与 Dependabot 依赖维护

## 不自动做的事项

- 不把1688搜索页最低展示价直接当采购价
- 不绕过验证码、登录或平台风控
- 不自动臆测材质、结构、套装数量和配件数量
- 不在缺少FBA、包装参数或有效采购价时生成伪完整投产比

## 正式使用前

1. 在输入模板补齐Amazon商品名称、中文搜索词、规格、包装参数和FBA。
2. 采集候选后，确认候选的完整销售单元采购价。
3. 对自动无法确认的候选填写“人工有效=是/否”及原因。
4. 运行计算并检查来源追溯表。

## 公开仓库检查

1. 不提交 Cookie、真实 ASIN 清单、报价、联系方式、截图或本机绝对路径。
2. 运行 `python -m pytest -q`、`python -m ruff check .` 和 `python -m ruff format --check .`。
3. 重新生成样例后，确认工作簿无隐藏表、外部链接和个人元数据。
4. 1688 或汇率接口变化时，先更新离线契约测试，再修改实现。
